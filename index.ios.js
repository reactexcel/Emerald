/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */
import React from "react-native";
import Root from "./src/containers/root";

const {AppRegistry} = React;

AppRegistry.registerComponent('emerald', () => Root);
